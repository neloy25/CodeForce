#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;
    while(t--){
        int n,m,x,y;
        cin >> n >> m >> x >> y;

        int res = 0;
        for(int i = 0; i < n; i++) {
            int val;
            cin >> val;
        }

        for(int i = 0; i < m; i++) {
            int val;
            cin >> val;
        }

        cout << n + m << endl;
    }
    return 0;
}

